function usuario(id, name, age, nickname) {
    return {
        id,
        name,
        age,
        nickname
    }
}

console.log(usuario(1, "juan", 19, "JdeManuel"));

// PODEMOS ESPECIFICAR LOS VALORES DE LOS OBJETOS SIN TENER QUE ESPECIFICARLOS, PODEMOS RECISIVIRLOS COMO LOS MANDAN DESDE LOS PARAMETROS